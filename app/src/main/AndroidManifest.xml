<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.clinic_project">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="com.example.clinic_project.permission.MAPS_RECEIVE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="Clinic"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Activity.NewCardViewActivity"></activity>
        <activity
            android:name=".Activity.AnotherHomenaviActivity"
            android:label="@string/title_activity_another_homenavi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.DoctorsByHospital" />
        <activity android:name=".Activity.NewActivity" />
        <activity android:name=".Activity.MyFavouriteHospitalActivity" />
        <activity android:name=".Activity.MyFavouriteDoctorActivity" />
        <activity android:name=".Activity.MyBookingActivity" />
        <activity android:name=".Activity.DepartmentActivity" />
        <activity android:name=".Activity.ServiceBookingActivity" />
        <activity android:name=".Activity.ExaminationDetailActivity" />
        <activity android:name=".Activity.RegistrationDetailActivity" />
        <activity android:name=".Activity.AssessmentDetailActivity" />
        <activity android:name=".Activity.DragDetailActivity" />

        <activity
            android:name=".Activity.ServiceActivity2"
            android:label="@string/title_activity_service2" />
        <activity android:name=".Activity.ServiceActivity" />
        <activity android:name=".Activity.DoctorsByClinic" />
        <activity android:name=".Activity.CalenderViewActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD7wedCmnXF9pCgX_2ConWyLSQOMwWUL1A" /> <!-- <meta-data -->
        <!-- android:name="com.google.android.maps.v2.API_KEY" -->
        <!-- android:value="@string/google_maps_key" /> -->
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Activity.PhoneActivity" />
        <activity
            android:name=".Activity.CalenderViewActivity"
            tools:ignore="DuplicateActivity" />

        <meta-data
            android:name="android.webkit.WebView.MetricsOptOut"
            android:value="false" />

        <activity android:name=".Activity.LabDetailActivity" />
        <activity android:name=".Activity.ClinicDetailActivity" />
        <activity android:name=".Activity.HospitalDetailActivity" />
        <activity android:name=".Activity.DoctorDetailActivity" />
        <activity android:name=".Activity.HospitalActivity" />
        <activity
            android:name=".Activity.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.HomenaviActivity" />
        <activity android:name=".Activity.ClinicActivity" />
        <activity android:name=".Activity.LabActivity" />
        <activity android:name=".Activity.DoctorListActivity" />
        <activity android:name=".Activity.DrawerActivity" />
        <activity android:name=".Activity.TopicActivity" />
        <activity android:name=".Activity.RegisterActivity" />
        <activity android:name=".Activity.HomeActivity" />
        <activity android:name=".Activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>